export const sayHello = (from) => {
	if (from) {
		// eslint-disable-next-line no-console
		console.log(`Hello world from ${from}`);
	} else {
		// eslint-disable-next-line no-console
		console.log('hello world');
	}
};
